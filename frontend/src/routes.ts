/**
 * Every destination the frontend links to.
 */

// --- GLOBALS ---
const PENDING = '/404'

export const ROUTES = {
  home: '/',
  explore: PENDING,
  write: PENDING,
  login: PENDING,
  register: PENDING,
} as const
